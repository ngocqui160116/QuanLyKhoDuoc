@using Phoenix.Server.Services.Database;
@model Phoenix.Server.Web.Areas.Admin.Models.Supplier.SupplierModel
@section Scripts{
    <script type="text/javascript" src="~/assets/js/tabulator.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
}
@section Breadcrumb{
    <div class="-intro-x breadcrumb mr-auto hidden sm:flex">
        <span>Chương trình</span> <i data-feather="chevron-right" class="breadcrumb__icon"></i>
        <a href="#" class="breadcrumb--active">Đối tác</a><i data-feather="chevron-right" class="breadcrumb__icon"></i>
        <a href="#" class="breadcrumb--active">Nhà cung cấp</a>
    </div>
}
@Html.AntiForgeryToken()
<div class="intro-y flex flex-col sm:flex-row items-center mt-8">
    <h2 class="text-lg font-medium mr-auto">
        Nhà cung cấp
    </h2>
    <div class="w-full sm:w-auto flex mt-4 sm:mt-0">
        <a href="@Url.Action("Create", "Supplier")" class="btn btn-primary text-white shadow-md mr-2">Thêm mới</a>
    </div>
</div>
<div class="grid gap-6 mt-5">
    <div class="intro-y box p-5">
        <div class="grid grid-cols-12 gap-2">
            <div class="col-span-12 lg:col-span-6">
                <label>Tên nhà cung cấp</label>
                <input type="text" id="Name" class="form-control">
            </div>
            <div class="col-span-12 lg:col-span-6">
                <label>Số điện thoại</label>
                <input type="text" id="PhoneNumber" class="form-control">
            </div>
        </div>
        <div class="grid mt-3">
            <div class="w-full sm:w-auto flex mt-5 sm:mt-0 ml-auto">
                <button type="button" onclick="onResetSearch();" class="btn btn-danger text-white shadow-md mr-2">Xóa tìm kiếm</button>
                <button type="button" onclick="onSearch();" class="btn btn-warning text-white shadow-md mr-2">Tìm kiếm</button>
            </div>
        </div>
    </div>
</div>
<div class="intro-y box p-5 mt-5">
    <div id="main-grid"></div>
    <script>
        var dateFormatter = function (cell, formatterParams) {
            var value = cell.getValue();

            if (value) {
                value = moment(value).format("DD/MM/YYYY HH:mm:ss");
            }

            return value;
        }
        var table = new Tabulator("#main-grid", {
            layout: "fitDataFill",
            ajaxURL: "@Html.Raw(Url.Action("List", "Supplier"))",
            ajaxConfig: "post",
            ajaxRequesting: function (url, params) {
                params.Name = $('#Name').val();
                params.PhoneNumber = $('#PhoneNumber').val();
            },
            ajaxResponse: function (url, params, response) {
                return response;
            },
            ajaxFiltering: true,
            paginationSize: 10,
            paginationSizeSelector: [10, 25, 50, 100],
            pagination: "remote",
            paginationDataSent: {
                "size": "pageSize",
                "page": "page",
            },
            paginationDataReceived: {
                "data": "Data",
                "last_page": "Total"
            },
            placeholder: "Chưa có dữ liệu",
            columns: [
                {
                    title: "Mã nhà cung cấp", field: "IdSupplier", sorter: "string", width: 180, formatter(cell, formatterParams) {
                        return `<a href="../Admin/Supplier/Update/${cell.getData().IdSupplier}" class="text-theme-1 dark:text-theme-10"> ${cell.getData().IdSupplier}</a>`
                    }
                },
                {
                    title: "Tên", field: "Name", sorter: "string", width: 180, formatter(cell, formatterParams)
                    {
                        return `<a href="../Admin/Supplier/Update/${cell.getData().IdSupplier}" class="text-theme-1 dark:text-theme-10"> ${cell.getData().Name}</a>`
                    }
                },
                {
                    title: "Số điện thoại", field: "PhoneNumber", sorter: "string", width: 180, formatter(cell, formatterParams) {
                        return `<a href="../Admin/Supplier/Update/${cell.getData().IdSupplier}" class="text-theme-1 dark:text-theme-10"> ${cell.getData().PhoneNumber}</a>`
                    }
                },
                {
                    title: "Email", field: "Email", sorter: "string", width: 200, formatter(cell, formatterParams) {
                        return `<a href="../Admin/Supplier/Update/${cell.getData().IdSupplier}" class="text-theme-1 dark:text-theme-10"> ${cell.getData().Email}</a>`
                    }
                },
                {
                    title: "Address", field: "Address", sorter: "string", width: 180, formatter(cell, formatterParams) {
                        return `<a href="../Admin/Supplier/Update/${cell.getData().IdSupplier}" class="text-theme-1 dark:text-theme-10"> ${cell.getData().Address}</a>`
                    }
                },
                {
                    title: "", field: "IdSupplier", sorter: "string", width: 100, formatter(cell, formatterParams) {
                        return `<a href="../Admin/Supplier/Update/${cell.getData().IdSupplier}" class="btn btn-primary text-white shadow-md mr-2">Sửa</a>`
                    }
                }
                @*{
                    title: "Ngày tạo", field: "CreatedAt", sorter: "date", width: 180, formatter: dateFormatter
                },
                {
                    title: "Ngày cập nhật", field: "UpdatedAt", sorter: "date", width: 180, formatter: dateFormatter
                },*@
            ],
        });
        let onSearch = (e) => {
            table.setData();
        }
        let onResetSearch = (e) => {
            $('#Name').val('');
            $('#PhoneNumber').val('');
            table.setData();
        }
    </script>
</div>

